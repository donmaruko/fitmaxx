# Core dependencies
numpy==1.26.4
pillow==10.3.0
matplotlib==3.8.4
tqdm==4.66.2
scikit-learn==1.4.2
joblib==1.4.2

# LangGraph / LangChain Core
langgraph==0.0.34
langchain-core==0.1.41
langchain==0.1.16
openai==1.23.2  # Needed for ChatOpenAI
python-dotenv==1.0.1

# Torch + torchvision (CPU version to be safe)
torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu
--index-url https://download.pytorch.org/whl/cpu

# MediaPipe (works on Windows)
mediapipe==0.10.9

# OpenAI CLIP (installed directly from GitHub)
git+https://github.com/openai/CLIP.git@dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1

# UMAP for embedding plots (optional but used in `outfitCLIPRunner.py`)
umap-learn==0.5.6
